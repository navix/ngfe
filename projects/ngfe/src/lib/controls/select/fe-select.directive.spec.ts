import { FeSelect } from './fe-select.directive';

describe('FeSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new FeSelect();
    expect(directive).toBeTruthy();
  });
});
