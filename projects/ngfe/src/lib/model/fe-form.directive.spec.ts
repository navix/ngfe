import { FeForm } from './fe-form.directive';

describe('FeFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FeForm();
    expect(directive).toBeTruthy();
  });
});
