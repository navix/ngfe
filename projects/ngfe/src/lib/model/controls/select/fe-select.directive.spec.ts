import { FeSelectDirective } from './fe-select.directive';

describe('FeSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new FeSelectDirective();
    expect(directive).toBeTruthy();
  });
});
