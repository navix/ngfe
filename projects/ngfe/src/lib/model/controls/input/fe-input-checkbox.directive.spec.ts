import { FeInputCheckboxDirective } from './fe-input-checkbox.directive';

describe('FeInputCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new FeInputCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
