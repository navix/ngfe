import { FeInputTextDirective } from './fe-input-text.directive';

describe('FeInputTextDirective', () => {
  it('should create an instance', () => {
    const directive = new FeInputTextDirective();
    expect(directive).toBeTruthy();
  });
});
