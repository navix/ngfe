import { FeInputRadioDirective } from './fe-input-radio.directive';

describe('FeInputRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new FeInputRadioDirective();
    expect(directive).toBeTruthy();
  });
});
