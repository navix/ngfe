import { FnValidatorDirective } from './fn-validator.directive';

describe('FnValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FnValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
