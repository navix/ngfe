import { FeFnValidatorDirective } from './fe-fn-validator.directive';

describe('FnValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FeFnValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
